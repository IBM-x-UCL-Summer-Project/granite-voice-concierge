# Dev dependencies (runtime + tooling) are declared in
# pyproject.toml [project.optional-dependencies].dev.
-e .[dev]

# Optional experiment extras
#-r experiments/wake-word-detection/requirements.txt
#-r experiments/vad-integration/requirements.txt
